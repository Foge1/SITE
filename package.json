{
  "name": "gruz-site",
  "version": "1.0.0",
  "private": true,
  "description": "Статический сайт услуг грузчиков с 11ty-сборкой",
  "scripts": {
    "dev": "eleventy --serve",
    "check:encoding": "node scripts/check-encoding.mjs --scope=source",
    "check:dist": "node scripts/check-encoding.mjs --scope=dist",
    "build": "npm run check:encoding && cross-env ELEVENTY_PATH_PREFIX=/ eleventy && npm run check:dist",
    "build:pages": "npm run check:encoding && cross-env ELEVENTY_PATH_PREFIX=/SITE/ eleventy && npm run check:dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cross-env": "^7.0.3"
  }
}
